<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Salus">
        <activity
            android:name=".HabitsActivity"
            android:exported="false"
            android:label="@string/title_activity_habits"
            android:theme="@style/Theme.Salus" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Salus">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InicialScreenActivity"
            android:exported="false"
            android:label="Tela Inicial"
            android:theme="@style/Theme.Salus"></activity>
        <activity
            android:name=".CompetitionConfigActivity"
            android:exported="false"
            android:label="Conffigurações de Competição"
            android:theme="@style/Theme.Salus"></activity>
        <activity
            android:name=".EachCompetitionActivity"
            android:exported="false"
            android:theme="@style/Theme.Salus" />
    </application>

</manifest>